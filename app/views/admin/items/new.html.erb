<div class="container">
  <div class="row my-3 pt-3">
    <div class="col-lg-3 ml-5">
      <h4 class="bg-light text-center">商品新規登録</h4>
    </div>
  </div>
</div>

<% if @item.errors.any? %>
  <%= @item.errors.count %>件のエラーが発生しました。
  <ul>
    <% @item.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<div class="container my-5">
  <%= form_with model: @item, url: admin_items_path do |f| %>
    <div class="form-group row justify-content-center">
      <div class="col-lg-2 col-form-label"><%= f.label "商品画像" %></div>
      <div class="col-lg-4">
        <%= f.file_field :image, accept: "image/*" %>
      </div>
    </div>

    <div class="form-group row justify-content-center">
      <div class="col-lg-2 col-form-label"><%= f.label "商品名" %></div>
      <div class="col-lg-4">
        <%= f.text_field :name, placeholder:"商品名", class:"form-control" %>
      </div>
    </div>

    <div class="form-group row justify-content-center">
      <div class="col-lg-2 col-form-label"><%= f.label "商品説明" %></div>
      <div class="col-lg-4">
        <%= f.text_area :introduction, placeholder:"ここに説明文を記述します", class:"form-control" %>
      </div>
    </div>

    <div class="form-group row justify-content-center">
      <div class="col-lg-2 col-form-label"><%= f.label "税抜価格" %></div>
      <div class="col-lg-4">
        <%= f.number_field :price, placeholder:"1000", class:"form-control" %>円
      </div>
    </div>


    <div class="row justify-content-center">
      <%= f.submit "新規登録", class: "btn btn-success mt-3" %>
    </div>
  <% end %>
</div>
