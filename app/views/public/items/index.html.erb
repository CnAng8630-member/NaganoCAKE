<h2>商品一覧</h2>(全<%= @items.count %>件)

<% @item.each do |item| %>
  <div>
    <%= link_to item_path(item.id) do %>
      <%= image_tag item.get_image(100,100) %>

      <p><%= item.name %></p>

      <p>¥<%= item.add_tax_price.to_s(:delimited) %></p>
    <% end %>
  </div>
<% end %>

<%= paginate @item %>